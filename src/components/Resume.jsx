export default function Resume({ title, image }) {
    return (
     <div className="resume">
        <h2>{title}</h2>
        {/* <img src={image} alt="resume" width="800" height="800"  /> */}
     </div>
    );
  }